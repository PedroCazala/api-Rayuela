import { IProduct } from "../interfaces/products.interface";

export class ShipmentsService {
    static async getTotalVolume(products: IProduct[]) {
        // const user = await UserDaoMongo.getOneById(idUser);

        // return user;
    }
}
